{% extends 'layeout.html.twig' %}
{% block title %}
les villes{% endblock %}
{% block main %}
<h3>Gérer les villes</h3>
    <div class="container">
        <div class="row">
            <div class="col-md-4 mr-auto">
                {% include 'admin/search.html.twig' with {form: form}  only %}

            </div>
        </div>
    </div>
    <br>


    <table class="table">
        <thead class="thead-dark">
        <tr>
            <th scope="col">Ville</th>

            <th scope="col">Action</th>
        </tr>
        </thead>

    <tbody>
    {% for campus in campus %}
    <tr>

    <td>{{campus.nom }}</td>


        <td><a href="{{ path('campus_update', {'id': campus.id}) }}">modifier</a>-<a href="{{ path('campus_delete', {'id': campus.id}) }}">supprimer</a></td>
    </tr>
    {% endfor %}
    </tbody>
    </table>

    <a class="navbar-brand" href="{{ path('admin_campus') }}">Ajouter un campus</a>
{% endblock %}